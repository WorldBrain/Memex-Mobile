import React from 'react';
import { Text } from "react-native";

interface Props {

}
export function TodoList(props : Props) {
    return (
        <Text>Only stateless components are supposed to live here</Text>
    )
}