import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    basicText: {
        fontSize: 16,
        textAlign: 'center',
        position: 'relative',
        top: 70,
        fontWeight: 'bold',
    },
})
