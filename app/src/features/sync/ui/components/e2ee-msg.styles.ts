import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    basicText: {
        fontSize: 16,
        textAlign: 'center',
    },
    textLink: {
        textDecorationLine: 'underline',
    },
})
