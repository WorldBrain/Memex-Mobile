import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    stepText: {
        fontSize: 18,
        textTransform: 'uppercase',
        fontWeight: 'bold',
        textAlign: 'center',
    },
})
