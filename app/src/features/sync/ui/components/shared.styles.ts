import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    stepText: {
        position: 'relative',
        top: 30,
        fontSize: 25,
        textTransform: 'uppercase',
        fontWeight: 'bold',
        textAlign: 'center',
    },
})
