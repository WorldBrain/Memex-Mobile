import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    dropdown: {
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        marginTop: 5,
    },
    resultsCount: {
        color: '#a2a2a2',
        marginRight: 5,
    },
    toggleIcon: {},
})
