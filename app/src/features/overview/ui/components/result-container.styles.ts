import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    result: {
        flex: 1,
        flexDirection: 'column',
        borderRadius: 4,
        borderWidth: 0.5,
        borderColor: '#d6d7da',
        marginTop: 15,
        padding: 10,
    },
})
