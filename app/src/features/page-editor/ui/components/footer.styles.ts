import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 0,
        borderTopWidth: 1,
        borderTopColor: '#dadada',
        height: 90,
        width: '100%',
    },
    mainText: {
        padding: 5,
        fontSize: 18,
        textAlign: 'center',
    },
})
