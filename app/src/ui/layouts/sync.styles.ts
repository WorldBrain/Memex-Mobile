import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    titleText: {
        top: 30,
        fontSize: 25,
        fontWeight: 'bold',
        textTransform: 'uppercase',
    },
    subtitleText: {
        fontSize: 18,
        marginVertical: 30,
        marginHorizontal: 50,
        textAlign: 'center',
    },
    children: {
        height: 300,
    },
})
